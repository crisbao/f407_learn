# STM32F407 智能家居 — 完整技术档案

> **生成日期**: 2026-07-29 | **版本**: v0.2 (Bare-metal) | **MCU**: STM32F407VET6 @ 168MHz
> **IDE**: Keil MDK | **HAL**: STM32F4 HAL Driver | **CubeMX**: v6.17.0 | **编译器**: ARMCC

---

## 项目身份证（300 字摘要）

本项目是基于 STM32F407VET6 + CubeMX HAL 库的智能家居原型，当前为**裸机版本**（FreeRTOS 未移植）。采用三层架构：**APP 应用层**（业务调度 / 传感器 / 显示 / 控制 / 协议 / 配置 / 系统状态）、**Hardware 硬件抽象层**（LED / DHT11 / OLED / HC05 / Flash / USART / 延时）、**Core + HAL 驱动层**（CubeMX 生成的外设初始化）。

主循环位于 `main.c` → `APP_Run()`，以设定间隔（默认 1000ms，最小 500ms，最大 60000ms）采集 DHT11 温湿度并通过 USART1 打印，同时轮询 HC-05 蓝牙环形缓冲区解析文本命令，最后按需刷新 OLED 多页面显示（HOME / SENSOR / SYSTEM / DEBUG）。命令协议支持 7 条指令。

**v0.2 核心升级**：实现了 Flash 配置持久化——配置参数经 Magic (0x53484F4D) + Version + Length + CRC32 + Value Range 五重校验后从 Sector7 (0x08060000) 加载，INTERVAL 命令修改后自动擦写保存。SYSTEM 页和 STATUS 命令动态显示 HC05/Config 来源（FLASH vs DEFAULT），不再硬编码。

已知缺陷：DHT11 `ReadBit()` 中 NULL 检查在解引用之后、HC-05 初始化不完整（未配置 KEY/STATE）、无 FreeRTOS、无看门狗、无传感器滤波。下一步优先移植 FreeRTOS。

---

## 🆕 自上一版（2026-07-26）以来的变更摘要

| 变更类型 | 文件 | 说明 |
|---------|------|------|
| ✨ **新增** | [APP/app_status.h](APP/app_status.h) | 新建 `APP_ConfigStatus_t` 枚举（8 种状态码：OK / ERROR_MAGIC / ERROR_VERSION / ERROR_LENGTH / ERROR_CRC / ERROR_VALUE / ERROR_FLASH_Erase / ERROR_FLASH_Write） |
| ✨ **新增** | [Hardware/flash.h](Hardware/flash.h) | 新建内部 Flash 驱动头文件（Sector7, 基址 0x08060000）——声明 `FLASH_EraseConfigSector()` / `FLASH_Write()` / `FLASH_Read()` |
| ✨ **新增** | [Hardware/flash.c](Hardware/flash.c) | 新建内部 Flash 驱动实现——扇区擦除（HAL_FLASHEx_Erase）→ 字写入（HAL_FLASH_Program, 按 4 字节对齐）→ 直接地址读取（memcpy） |
| 🔧 **重写** | [APP/app_config.c](APP/app_config.c) | 从空壳升级为完整方案：新增软件 CRC32 计算（多项式 0xEDB88320）、参数范围校验（500~60000ms）、Flash 五重校验加载（Magic→Version→Length→CRC→Value）、Flash 擦写保存；字段重命名 `sensorInterval` → `sensorIntervalMs` |
| 🔧 **扩展** | [APP/app_config.h](APP/app_config.h) | 新增 `#include "app_status.h"`、Flash 基址宏 `APP_CONFIG_FLASH_ADDRESS`(0x08060000)、Magic 号 `APP_CONFIG_MAGIC`(0x53484F4D = "SHOM")、Version `APP_CONFIG_VERSION`(1)、`APP_ConfigStorage_t` 结构体（magic+version+length+data+crc）；`APP_Config_Load/Save` 返回值从 void → `APP_ConfigStatus_t` |
| 🔧 **扩展** | [APP/app_system.h](APP/app_system.h) | 新增 `#include "app_status.h"`，新增 `APP_System_SetConfigStatus()` / `APP_System_GetConfigStatus()` 声明 |
| 🔧 **扩展** | [APP/app_system.c](APP/app_system.c) | 新增 `static APP_ConfigStatus_t configStatus` 变量，初始化值为 `APP_CONFIG_ERROR_MAGIC`；新增 Set/Get 实现 |
| 🔧 **修改** | [APP/app_display.c](APP/app_display.c) | `#include` 新增 `app_status.h` + `app_system.h`；SYSTEM 页 HC05 行由硬编码 `"Connected"` 改为动态 `APP_System_GetBTStatus() ? "OK" : "ERR"`；新增配置状态行 `CFG: OK/ERR`（`APP_System_GetConfigStatus()`）；移除旧的 FreeRTOS 硬编码行 |
| 🔧 **修改** | [APP/app_protocol.c](APP/app_protocol.c) | INTERVAL 命令增加 `APP_Config_Save()` 调用 + 保存结果反馈（OK / Save Failed），同时向 USART1 打印调试日志；STATUS 命令新增 CONFIG 来源行（FLASH / DEFAULT） |
| 📝 **微调** | [Hardware/usart_driver.h](Hardware/usart_driver.h) | 仅尾部追加一个空行（格式变更，无功能影响） |

**未变更的文件**：`app.c`、`main.c`、`stm32f4xx_it.c`、`gpio.c`、`i2c.c`、`usart.c`、所有 Hardware/*.c 驱动、所有 Core Inc 头文件、`f407_learn.ioc`。

---

## 1. 目录结构树（含职责说明）

```
f407_learn/                                  # 项目根目录
│
├── f407_learn.ioc                           # CubeMX 项目配置（12 引脚 / 6 个 IP / USART3 中断使能）
├── .gitignore                               # Git 忽略规则（构建产物 / 日志 / IDE 配置）
├── .mxproject                               # CubeMX 工程元数据
│
├── APP/                                     # 【应用层】业务逻辑，不直接操作 HAL
│   ├── app.c / app.h                        #   主调度器：APP_Init() 串联 7 个子模块初始化，APP_Run() 驱动主循环
│   ├── app_status.h                         #   🆕 全局状态码枚举：APP_ConfigStatus_t（8 种 Flash 操作状态）
│   ├── app_config.c / app_config.h          #   🔧 运行参数管理：Flash 持久化（Magic+Version+CRC32 五重校验）+ 范围校验
│   ├── app_system.c / app_system.h          #   🔧 全局状态：启动时间戳、DHT状态、蓝牙状态、配置加载状态
│   ├── app_sensor.c / app_sensor.h          #   传感器业务：DHT11 采集 + USART1 打印 + DHT 状态同步
│   ├── app_display.c / app_display.h        #   🔧 OLED 4 页切换 + 脏数据检测 + 动态系统状态显示
│   ├── app_control.c / app_control.h        #   执行器控制：LED 开关 + 状态查询
│   └── app_protocol.c / app_protocol.h      #   🔧 蓝牙文本命令解析器：7 命令 + INTERVAL 保存反馈 + STATUS 配置来源
│
├── Hardware/                                # 【硬件抽象层】外设驱动封装，不依赖 APP 层（hc05.c 例外）
│   ├── led.c / led.h                        #   LED（PC13，低电平点亮）—— 开/关/翻转/读取
│   ├── dht11.c / dht11.h                    #   DHT11 单总线驱动（PA3）—— 位带时序 + DWT 微秒延时
│   ├── oled.c / oled.h                      #   SSD1306 OLED 驱动（I2C1）—— GRAM 双缓冲 + 脏标记刷新
│   ├── oledfont.c / oledfont.h              #   8×16 ASCII 字库数据（F8x16 数组）
│   ├── hc05.c / hc05.h                      #   HC-05 蓝牙模块（USART3）—— 中断接收 + 128B 环形缓冲区
│   ├── usart_driver.c / usart_driver.h      #   UART 通用驱动 —— 阻塞发送 / printf 格式化 / 接收
│   ├── flash.c / flash.h                    #   🆕 内部 Flash 驱动 —— Sector7 擦除 / 字写入（4 字节对齐）/ 直接读取
│   ├── delay.c / delay.h                    #   DWT 周期计数器微秒延时 + HAL_Delay 毫秒延时
│   └── retarget.c                           #   printf 重定向到 USART1（fputc → HAL_UART_Transmit）
│
├── Core/                                    # 【平台层】CubeMX 生成的核心文件
│   ├── Src/
│   │   ├── main.c                           #   入口：HAL_Init → Clock → 外设初始化 → APP_Init → while(APP_Run)
│   │   ├── stm32f4xx_it.c                   #   中断服务函数（SysTick / USART3 / 6 系统异常 + 4 Fault）
│   │   ├── stm32f4xx_hal_msp.c             #   HAL MSP 初始化（时钟使能、NVIC 配置）
│   │   ├── system_stm32f4xx.c               #   系统初始化（FPU / 向量表 / 时钟配置前奏）
│   │   ├── gpio.c                           #   MX_GPIO_Init —— 仅 PC13 配置为推挽输出
│   │   ├── i2c.c                            #   MX_I2C1_Init —— PB6(SCL) / PB7(SDA), 100kHz
│   │   └── usart.c                          #   MX_USART1_UART_Init(115200) + MX_USART3_UART_Init(9600)
│   └── Inc/
│       ├── main.h                           #   主头文件（含 HAL 包含 + Error_Handler 声明）
│       ├── stm32f4xx_it.h                   #   中断函数声明
│       ├── stm32f4xx_hal_conf.h            #   HAL 模块裁剪开关
│       ├── gpio.h / i2c.h / usart.h         #   外设句柄 extern 声明（huart1, huart3, hi2c1）
│       └── stm32f407xx.h                    #   芯片寄存器定义（CMSIS）
│
├── Drivers/                                 # 【驱动库】STM32F4 HAL + CMSIS（只读，不修改）
│   ├── CMSIS/
│   │   └── Device/ST/STM32F4xx/Source/Templates/arm/
│   │       └── startup_stm32f407xx.s        #   启动文件：向量表 + Reset_Handler + Default_Handler + 堆栈定义
│   └── STM32F4xx_HAL_Driver/
│       ├── Src/                             #   HAL 库源码（stm32f4xx_hal_flash.c / hal_flash_ex.c 等 ~20 个 .c）
│       └── Inc/                             #   HAL 库头文件
│
├── MDK-ARM/                                 # Keil 工程目录
│   ├── f407_learn.uvprojx                   #   Keil 工程文件
│   ├── f407_learn/
│   │   ├── f407_learn.hex                   #   烧录文件
│   │   ├── f407_learn.axf                   #   调试文件
│   │   ├── f407_learn.map                   #   链接映射（函数/变量地址）
│   │   └── f407_learn.sct                   #   分散加载脚本（Flash: 0x08000000 512KB / RAM1: 0x20000000 112KB / RAM2: 0x2001C000 16KB）
│   └── *.o / *.d / *.crf                    #   编译中间产物（含 🆕 flash.o）
│
├── data/                                    # 运行时数据
│   └── pythagora.log                        #   Pythagora VSCode 插件日志（非项目代码）
│
└── .skill/                                  # 项目 AI 协作规范
    └── 00_global.skill.md                   #   编码规范 / 架构约束 / 迁移目标（FreeRTOS）
```

---

## 2. 模块依赖图（文件级 #include 关系）

```
                        ┌─────────────────┐
                        │  main.c         │
                        │  (入口)          │
                        └───────┬─────────┘
            ┌───────────────────┼───────────────────┐
            ▼                   ▼                   ▼
    ┌──────────┐      ┌──────────────┐      ┌──────────┐
    │ app.h    │      │ delay.h      │      │ gpio.h   │
    └────┬─────┘      └──────┬───────┘      │ i2c.h    │
         │                   │              │ usart.h  │
         │              ┌────┴────┐         └──────────┘
         │              │ main.h  │
         │              └─────────┘
         │
    ┌────┴────────────────────────────┐
    │         app.c                   │
    │   (串联所有 APP 子模块)          │
    └────┬────────────────────────────┘
         │
         ├──► app_system.c ◄── app_system.h
         │         │
         │         ├── stm32f4xx_hal.h  (HAL_GetTick)
         │         └── app_status.h     🆕 APP_ConfigStatus_t 枚举
         │
         ├──► app_config.c ◄── app_config.h
         │         │
         │         ├──► app_status.h     🆕 配置状态枚举
         │         ├──► flash.h          🆕 Flash 驱动接口
         │         │         └──► flash.c  🆕 HAL_FLASHEx_Erase / HAL_FLASH_Program
         │         ├──► app_system.h     写入配置加载状态
         │         ├── string.h          memcpy
         │         └── stdint.h
         │
         ├──► app_sensor.c ◄── app_sensor.h
         │         │
         │         ├──► dht11.h ◄── dht11.c
         │         │         │
         │         │         └──► delay.h  ◄── delay.c
         │         │         │                    │
         │         │         │              ┌─────┴─────┐
         │         │         │              │ main.h    │
         │         │         │              │ core_cm4.h│  (DWT 寄存器)
         │         │         │              └───────────┘
         │         │         │
         │         │         └── main.h
         │         │
         │         ├──► app_system.h       更新 DHT 状态
         │         ├──► usart_driver.h ◄── usart_driver.c
         │         │         │
         │         │         └──► usart.h  ◄── usart.c
         │         │
         │         └── string.h           memset
         │
         ├──► app_display.c ◄── app_display.h
         │         │
         │         ├──► app_sensor.h       获取传感器数据
         │         ├──► app_control.h      获取 LED 状态
         │         ├──► app_system.h       🆕 获取 BT 状态 / Config 状态
         │         ├──► app_status.h       🆕 判断 APP_CONFIG_OK
         │         └──► oled.h ◄── oled.c
         │                       │
         │                       ├──► i2c.h
         │                       ├──► oledfont.h ◄── oledfont.c
         │                       ├── string.h / stdarg.h / stdio.h
         │                       └── main.h
         │
         ├──► app_control.c ◄── app_control.h
         │         │
         │         └──► led.h ◄── led.c
         │                       │
         │                       └── main.h
         │
         └──► app_protocol.c ◄── app_protocol.h
                   │
                   ├──► app_control.h   LED 命令 → LED 控制
                   ├──► app_sensor.h    STATUS 命令 → 读取传感器
                   ├──► app_display.h   PAGE/OLED 命令 → 显示切换
                   ├──► app_config.h    🆕 INTERVAL 命令 → Save + 结果反馈
                   ├──► app_system.h    🆕 STATUS 命令 → CONFIG 来源 / BT 状态
                   ├──► usart_driver.h  🆕 INTERVAL 保存时向 USART1 打印调试日志
                   ├──► hc05.h ◄── hc05.c
                   │         │
                   │         ├──► usart_driver.h
                   │         ├──► usart.h
                   │         ├──► app_system.h  蓝牙状态更新（历史债务）
                   │         └── string.h
                   │
                   ├── string.h / stdlib.h
                   └── main.h

                        ┌──────────────────┐
                        │  retarget.c      │  (独立模块)
                        │  printf → USART1 │
                        └──────┬───────────┘
                               │
                          ┌────┴────┐
                          │ stdio.h │
                          │ usart.h │
                          └─────────┘


        🆕 v0.2 新增依赖链:
        ┌─────────────────────────────────────────┐
        │  app_config.c ──► flash.c               │
        │       │               │                  │
        │       │               ├─ HAL_FLASH_Unlock/Lock
        │       │               ├─ HAL_FLASHEx_Erase  (Sector7)
        │       │               ├─ HAL_FLASH_Program   (Word)
        │       │               └─ memcpy              (直接地址读取)
        │       │                                   │
        │       └──► app_status.h ◄── app_system.h ◄── app_display.c
        │                                          ◄── app_protocol.c
        └─────────────────────────────────────────┘
```

**依赖原则**:
- APP 层 **不在彼此之间横向依赖**（除读取数据指针/状态）
- Hardware 层 **不依赖 APP 层**（`hc05.c` 调用 `APP_System_SetBTStatus` 是已知历史债务）
- `flash.h/c` 只依赖 `stdint.h` + `stm32f4xx_hal.h` + `string.h`，无上层依赖
- `app_status.h` 是纯枚举头文件，被 `app_config.h` / `app_system.h` / `app_display.c` / `app_protocol.c` 包含
- 所有模块最终依赖 `main.h` → `stm32f4xx_hal.h`

---

## 3. 函数调用链

### 3.1 初始化调用链（启动 → APP_Init 完成）

```
上电 Reset
  │
  ▼
Reset_Handler (startup_stm32f407xx.s:169)
  ├─ SystemInit()                          [system_stm32f4xx.c]   FPU / 向量表偏移 / 时钟预处理
  └─ __main()                              [C 库]                 堆栈初始化 → 跳转 main()
       │
       ▼
main()                                      [Core/Src/main.c:71]
  │
  ├─ HAL_Init()                            [stm32f4xx_hal.c]     SysTick 配置 / NVIC 优先级分组
  │
  ├─ SystemClock_Config()                  [main.c:122]
  │    ├─ __HAL_RCC_PWR_CLK_ENABLE()
  │    ├─ __HAL_PWR_VOLTAGESCALING_CONFIG()                      调压器 Scale 1
  │    ├─ HAL_RCC_OscConfig()                                     HSE(8MHz) → PLL(M=8/N=336/P=2/Q=4) → 168MHz
  │    └─ HAL_RCC_ClockConfig()                                   SYSCLK=168M, HCLK=168M, PCLK1=42M, PCLK2=84M
  │
  ├─ MX_GPIO_Init()                        [Core/Src/gpio.c]     PC13 → 推挽输出（初始 LOW）
  ├─ MX_USART1_UART_Init()                 [Core/Src/usart.c]     PA9(TX)/PA10(RX), 115200-8-N-1
  ├─ MX_I2C1_Init()                        [Core/Src/i2c.c]      PB6(SCL)/PB7(SDA), 100kHz, AF_OD
  ├─ MX_USART3_UART_Init()                 [Core/Src/usart.c]     PB10(TX)/PB11(RX), 9600-8-N-1
  │    └─ HAL_UART_MspInit(&huart3)                              NVIC: USART3_IRQn, 优先级 0:0, 使能中断
  │
  ├─ Delay_Init()                          [Hardware/delay.c:6]
  │    ├─ CoreDebug->DEMCR |= TRCENA       使能 DWT 跟踪
  │    ├─ DWT->CYCCNT = 0                  清零计数器
  │    ├─ DWT->CTRL |= CYCCNTENA           使能周期计数器
  │    └─ us_factor = SystemCoreClock/1M   计算每微秒周期数
  │
  └─ APP_Init()                            [APP/app.c:17]
       │
       ├─ APP_System_Init()                                   [app_system.c:12]
       │    ├─ systemStartTick = HAL_GetTick()
       │    ├─ dhtStatus = HAL_ERROR
       │    ├─ btStatus = 0
       │    └─ 🆕 configStatus = APP_CONFIG_ERROR_MAGIC      初始假设 Flash 无有效配置
       │
       ├─ APP_Config_Init()                                    [app_config.c:19]
       │    ├─ APP_Config_Reset()                               传感器间隔 = APP_CONFIG_DEFAULT_INTERVAL (1000ms)
       │    ├─ 🆕 status = APP_Config_Load()                    尝试从 Flash Sector7 恢复参数
       │    │    │
       │    │    ├─ FLASH_Read(FLASH_CONFIG_ADDRESS, &storage, sizeof(storage))
       │    │    │    └─ 🆕 memcpy(data, (uint8_t*)0x08060000, length)  直接指针解引用
       │    │    │
       │    │    ├─ 🆕 [Magic 校验]    storage.magic != APP_CONFIG_MAGIC (0x53484F4D) → ERROR_MAGIC
       │    │    ├─ 🆕 [Version 校验]  storage.version != APP_CONFIG_VERSION (1)     → ERROR_VERSION
       │    │    ├─ 🆕 [Length 校验]   storage.length != sizeof(APP_ConfigData_t)    → ERROR_LENGTH
       │    │    ├─ 🆕 [CRC32 校验]    APP_Config_CRC32(data, length) ≠ storage.crc  → ERROR_CRC
       │    │    │    └─ 🆕 软件 CRC32：多项式 0xEDB88320, 逐字节 8 次移位
       │    │    ├─ 🆕 [Value 校验]    APP_Config_Check(&storage.data)
       │    │    │    └─ 🆕 sensorIntervalMs: 500 ≤ x ≤ 60000 ? 合法 : 非法
       │    │    └─ [全部通过] → memcpy(&appConfig, &storage.data, sizeof)
       │    │
       │    └─ 🆕 APP_System_SetConfigStatus(status)              将加载结果写入全局状态
       │
       ├─ APP_Sensor_Init()                                    [app_sensor.c:9]
       │    ├─ DHT11_Init()                                     [dht11.c:40]
       │    │    └─ DHT11_Pin_Input()                            GPIOA_PIN3 → 输入 + 上拉
       │    └─ memset(&sensorData, 0, sizeof(sensorData))
       │
       ├─ APP_Control_Init()                                   [app_control.c:9]
       │    ├─ LED_Off()                                        [led.c:25]
       │    │    └─ HAL_GPIO_WritePin(GPIOC, PIN13, SET)        熄灭（高电平）
       │    └─ ledState = APP_LED_OFF
       │
       ├─ APP_Display_Init()                                   [app_display.c:25]
       │    ├─ OLED_Init()                                      [oled.c:101]
       │    │    ├─ HAL_Delay(100)                              上电等待
       │    │    ├─ OLED_WriteCmd(...) × 25                      SSD1306 初始化序列
       │    │    │    └─ HAL_I2C_Master_Transmit(&hi2c1, 0x78, buf, 2, HAL_MAX_DELAY)
       │    │    ├─ OLED_Clear()                                memset(GRAM, 0) + dirty=1
       │    │    └─ OLED_Refresh()                              dirty=0，跳过（GRAM 已全黑）
       │    ├─ OLED_Clear()                                     再次清空
       │    ├─ firstRefresh = 1 / currentPage = DISPLAY_PAGE_HOME
       │    ├─ OLED_ShowString(0, 0, " Smart Home")
       │    │    └─ OLED_ShowChar() × 11                        ▶ 逐字写入 GRAM[page][col]
       │    └─ OLED_Refresh()
       │         └─ OLED_Update()                                ▶ 8 页 × I2C 块写入 SSD1306
       │
       └─ APP_Protocol_Init()                                  [app_protocol.c:33]
            └─ HC05_Init()                                      [hc05.c:29]
                 ├─ HC05_BufferInit()                            head=0, tail=0, buffer 清零
                 ├─ HC05_StartReceive()
                 │    └─ HAL_UART_Receive_IT(&huart3, &HC05_RxByte, 1)  启动 USART3 中断接收
                 └─ APP_System_SetBTStatus(1)                   标记蓝牙已就绪
```

### 3.2 主循环调用链（while(1) → APP_Run 迭代）

```
while(1) in main()
  │
  └─ APP_Run()                                     [APP/app.c:38]
       │
       │  ═══ 分支 1: 定时传感器采集 ═══
       │
       ├─ [条件: HAL_GetTick() - sensorTick >= APP_Config_GetSensorInterval()]
       │    │
       │    ├─ sensorTick = HAL_GetTick()
       │    │
       │    └─ APP_Sensor_Update()                                    [app_sensor.c:21]
       │         │
       │         └─ DHT11_Read(&sensorData)                           [dht11.c:215]
       │              │
       │              ├─ DHT11_Start()                                 [dht11.c:45]
       │              │    ├─ DHT11_Pin_Output()                       GPIOA3 → 推挽输出
       │              │    ├─ DHT11_LOW()                              拉低总线
       │              │    ├─ Delay_ms(20)                             HAL_Delay(20ms)
       │              │    ├─ DHT11_HIGH()                             释放总线
       │              │    ├─ Delay_us(30)                             DWT 微秒延时
       │              │    └─ DHT11_Pin_Input()                        GPIOA3 → 输入上拉
       │              │
       │              ├─ DHT11_CheckResponse()                         [dht11.c:119]
       │              │    └─ DHT11_WaitLevel() × 3                    ▶ DWT 循环等待低→高→低
       │              │
       │              ├─ DHT11_ReadByte(&buffer[0..4]) × 5             [dht11.c:184]
       │              │    └─ DHT11_ReadBit() × 8                      [dht11.c:153]
       │              │         ├─ DHT11_WaitLevel(HIGH, 150us)        ▶ 等上升沿
       │              │         ├─ Delay_us(40)                        采样点延时
       │              │         ├─ *bit = DHT11_IS_HIGH() ? 1 : 0      ⚠ BUG: bit==NULL 检查在解引用之后(line 165)
       │              │         └─ DHT11_WaitLevel(LOW, 150us)         ▶ 等下降沿
       │              │
       │              ├─ DHT11_Checksum(buffer)                        [dht11.c:67]
       │              │    └─ buffer[0]+[1]+[2]+[3] == buffer[4] ?
       │              │
       │              └─ 解析: humidity= buf[0].buf[1], temperature= buf[2].buf[3]
       │
       │         ├─ APP_System_SetDHTStatus(ret)                       更新全局 DHT 状态
       │         │
       │         └─ USART_Printf(&huart1, "ret=%d Temp=...")           [usart_driver.c:84]
       │              ├─ vsnprintf(buffer, 128, fmt, args)             格式化字符串
       │              └─ USART_SendString(&huart1, buffer)
       │                   └─ HAL_UART_Transmit(&huart1, str, len, HAL_MAX_DELAY)   ▶ 阻塞发送
       │
       │  ═══ 分支 2: 蓝牙协议处理（每次循环都执行） ═══
       │
       ├─ APP_Protocol_Process()                                       [app_protocol.c:41]
       │    │
       │    └─ while(HC05_BufferRead(&ch) == 0)                        [hc05.c:98]
       │         │    (环形缓冲区 head != tail 时逐个取出)
       │         │
       │         └─ APP_Protocol_Input(ch)                             [app_protocol.c:344]
       │              │
       │              ├─ ch == '\r' → APP_CmdBuffer[index] = '\0'
       │              │    └─ [index > 0] → APP_Protocol_Parse()       [app_protocol.c:302]
       │              │         │
       │              │         ├─ strchr(cmd, ' ')                    空格分割命令+参数
       │              │         │
       │              │         └─ 遍历 APP_CommandTable[] 匹配:
       │              │              │
       │              │              ├─ "LED"    → APP_Cmd_LED(param)
       │              │              │    ├─ strcmp(param,"ON")  → APP_Control_LED(APP_LED_ON)
       │              │              │    │    └─ LED_On() → HAL_GPIO_WritePin(GPIOC, PIN13, RESET)
       │              │              │    ├─ strcmp(param,"OFF") → APP_Control_LED(APP_LED_OFF)
       │              │              │    │    └─ LED_Off() → HAL_GPIO_WritePin(GPIOC, PIN13, SET)
       │              │              │    └─ HC05_Printf("OK"/"ERR")
       │              │              │
       │              │              ├─ "OLED"   → APP_Cmd_OLED(param)
       │              │              │    └─ strcmp(param,"CLEAR") → APP_Display_Clear()
       │              │              │         └─ OLED_Clear() + OLED_Refresh()
       │              │              │
       │              │              ├─ "OLED CLR" → APP_Cmd_OLED_Clear()
       │              │              │    └─ 同上
       │              │              │
       │              │              ├─ "TEST"   → APP_Cmd_Test()
       │              │              │    └─ HC05_Printf("OK\r\n")
       │              │              │
       │              │              ├─ "STATUS" → APP_Cmd_Status()              [app_protocol.c:223]
       │              │              │    ├─ 🆕 cfg = APP_System_GetConfigStatus()  读取配置加载状态
       │              │              │    ├─ APP_Sensor_GetData()                   获取传感器数据指针
       │              │              │    ├─ APP_System_GetUptime()                 (HAL_GetTick - startTick)/1000
       │              │              │    ├─ APP_System_GetDHTStatus()              获取 DHT 状态
       │              │              │    ├─ APP_System_GetBTStatus()               获取蓝牙状态
       │              │              │    ├─ 🆕 cfg == APP_CONFIG_OK ? "CONFIG:FLASH" : "CONFIG:DEFAULT"
       │              │              │    ├─ APP_Control_GetLEDState()              获取 LED 状态
       │              │              │    ├─ APP_Display_GetPage()                  获取当前页面
       │              │              │    ├─ APP_Config_GetSensorInterval()         获取采样间隔
       │              │              │    └─ HC05_Printf() × 12                     逐行返回状态报告
       │              │              │
       │              │              ├─ "PAGE"   → APP_Cmd_Page()
       │              │              │    ├─ strcmp → APP_Display_SetPage(HOME/SENSOR/SYSTEM/DEBUG)
       │              │              │    └─ HC05_Printf("PAGE %s OK")
       │              │              │
       │              │              └─ "INTERVAL" → APP_Cmd_SetInterval()          [app_protocol.c:154] 🔧
       │              │                   ├─ atoi(param)                            字符串转整数
       │              │                   ├─ [sec == 0] → "ERR VALUE"
       │              │                   ├─ APP_Config_SetSensorInterval(sec * 1000)
       │              │                   │    └─ 🆕 [ms < 500] → ms = 500, 写入 appConfig.sensorIntervalMs
       │              │                   ├─ 🆕 ret = APP_Config_Save()             写入 Flash
       │              │                   │    │
       │              │                   │    ├─ 🆕 APP_Config_Check(&appConfig)     再次校验参数合法性
       │              │                   │    ├─ 🆕 构建 storage = {magic, version, length, data, crc}
       │              │                   │    ├─ 🆕 FLASH_EraseConfigSector()         擦除 Sector7
       │              │                   │    │    ├─ HAL_FLASH_Unlock()
       │              │                   │    │    ├─ HAL_FLASHEx_Erase(SECTOR_7, 1, VoltageRange_3)
       │              │                   │    │    └─ HAL_FLASH_Lock()
       │              │                   │    └─ 🆕 FLASH_Write(addr, &storage, sizeof)  写入数据
       │              │                   │         ├─ HAL_FLASH_Unlock()
       │              │                   │         ├─ while(length): 按 4 字节对齐 HAL_FLASH_Program(WORD)
       │              │                   │         └─ HAL_FLASH_Lock()
       │              │                   │
       │              │                   ├─ 🆕 [ret == OK] → HC05_Printf("INTERVAL=%lu s OK")
       │              │                   │                + USART_Printf(&huart1, "Config Saved...")
       │              │                   └─ 🆕 [ret ≠ OK] → HC05_Printf("Save Failed")
       │              │                                    + USART_Printf(&huart1, "Config Save Failed...")
       │              │
       │              ├─ ch == '\n' → return (忽略)
       │              │
       │              └─ 普通字符 → APP_CmdBuffer[APP_CmdIndex++] = ch
       │                   └─ [溢出] → index=0, 清空, HC05_Printf("ERROR: Too Long")
       │
       │  ═══ 分支 3: 显示更新（每次循环都执行） ═══
       │
       └─ APP_Display_Update()                                         [app_display.c:194]
            │
            ├─ APP_Display_IsChanged()                                 [app_display.c:46]
            │    ├─ firstRefresh == 1 ? → 首次必刷新
            │    ├─ currentPage != lastPage ? → 页面切换必刷新
            │    ├─ sensor->temperature != lastSensor.temperature ?
            │    ├─ sensor->humidity != lastSensor.humidity ?
            │    └─ APP_Control_GetLEDState() != lastLedState ?
            │
            ├─ [无变化] → return (跳过)
            │
            └─ [有变化]
                 │
                 ├─ OLED_Clear()                                       清空 GRAM + dirty=1
                 │
                 ├─ switch(currentPage):
                 │    ├─ DISPLAY_PAGE_HOME   → APP_Display_ShowHomePage()
                 │    │    ├─ OLED_ShowString("Smart Home")
                 │    │    ├─ OLED_Printf("Temp:%d.%d C", ...)
                 │    │    ├─ OLED_Printf("Humi:%d.%d %%", ...)
                 │    │    └─ OLED_ShowString("LED : ON/OFF")
                 │    │
                 │    ├─ DISPLAY_PAGE_SENSOR → APP_Display_ShowSensorPage()
                 │    │    ├─ OLED_ShowString("Sensor")
                 │    │    └─ OLED_Printf("Temp/Humi...") × 2
                 │    │
                 │    ├─ DISPLAY_PAGE_SYSTEM → APP_Display_ShowSystemPage()    🔧
                 │    │    ├─ OLED_ShowString("System")
                 │    │    ├─ OLED_Printf("LED : %s", ON/OFF)
                 │    │    ├─ 🔧 OLED_Printf("HC05: %s", APP_System_GetBTStatus() ? "OK" : "ERR")
                 │    │    └─ 🔧 OLED_Printf("CFG: %s", APP_System_GetConfigStatus() == APP_CONFIG_OK ? "OK" : "ERR")
                 │    │         (旧版为硬编码 "HC05: Connected" + "FreeRTOS: OFF" — 已移除)
                 │    │
                 │    └─ DISPLAY_PAGE_DEBUG  → APP_Display_ShowDebugPage()
                 │         ├─ OLED_Printf("Tick:%lu", HAL_GetTick())
                 │         ├─ OLED_ShowString("Mode: Bare")
                 │         └─ OLED_ShowString("Ready")
                 │
                 ├─ OLED_Refresh()                                     [oled.c:183]
                 │    └─ [OLED_Dirty == 1]
                 │         └─ OLED_Update()                            [oled.c:158]
                 │              └─ for(page=0; page<8; page++):
                 │                   ├─ OLED_WriteCmd(PAGE_ADDR+page)   ▶ I2C 写 2 字节
                 │                   ├─ OLED_WriteCmd(COLUMN_LOW)
                 │                   ├─ OLED_WriteCmd(COLUMN_HIGH)
                 │                   └─ OLED_WriteData(GRAM[page], 128) ▶ I2C 写 129 字节(0x40 + 128)
                 │
                 └─ APP_Display_SaveState()                            快照当前数据用于下次比对
```


### 3.3 中断调用链

```
USART3_IRQHandler()                           [Core/Src/stm32f4xx_it.c:204]
  │
  └─ HAL_UART_IRQHandler(&huart3)             [stm32f4xx_hal_uart.c]
       │
       └─ [接收完成]
            └─ HAL_UART_RxCpltCallback()       [Hardware/hc05.c:123]
                 │
                 ├─ [huart->Instance == USART3]
                 │    └─ HC05_BufferWrite(HC05_RxByte)     存入环形缓冲区
                 │         └─ head = (head+1) % 128
                 │
                 └─ HAL_UART_Receive_IT(&huart3, &HC05_RxByte, 1)    重新启动下一次接收


SysTick_Handler()                             [Core/Src/stm32f4xx_it.c:183]
  │
  └─ HAL_IncTick()                            [stm32f4xx_hal.c]
       └─ uwTick++                            (供 HAL_GetTick / HAL_Delay 使用)
```

---

## 4. 中断向量表及对应的 ISR 实现位置

### 启动文件栈大小

> **`Stack_Size = 0x00000400` (1024 bytes)** — [startup_stm32f407xx.s:32](Drivers/CMSIS/Device/ST/STM32F4xx/Source/Templates/arm/startup_stm32f407xx.s#L32)
> `Heap_Size = 0x00000200` (512 bytes) — [startup_stm32f407xx.s:43](Drivers/CMSIS/Device/ST/STM32F4xx/Source/Templates/arm/startup_stm32f407xx.s#L43)

### 中断向量表

| 向量偏移 | 中断名 | NVIC 使能 | 启动文件弱定义 | 用户实现位置 | 功能说明 |
|---------|--------|----------|-------------|------------|---------|
| 0x0000 | `__initial_sp` | — | startup.s:34 | — | 栈顶指针（0x20000400） |
| 0x0004 | `Reset_Handler` | — | startup.s:169 | `SystemInit()` + `__main()` → `main()` | 复位入口 |
| 0x0008 | `NMI_Handler` | IOC:true | startup.s:182 | **[覆写]** [stm32f4xx_it.c:69](Core/Src/stm32f4xx_it.c#L69) → `while(1)` | 不可屏蔽中断 |
| 0x000C | `HardFault_Handler` | IOC:true | startup.s:186 | **[覆写]** [stm32f4xx_it.c:84](Core/Src/stm32f4xx_it.c#L84) → `while(1)` | 硬件错误 |
| 0x0010 | `MemManage_Handler` | IOC:true | startup.s:191 | **[覆写]** [stm32f4xx_it.c:99](Core/Src/stm32f4xx_it.c#L99) → `while(1)` | 内存管理错误 |
| 0x0014 | `BusFault_Handler` | IOC:true | startup.s:196 | **[覆写]** [stm32f4xx_it.c:114](Core/Src/stm32f4xx_it.c#L114) → `while(1)` | 总线错误 |
| 0x0018 | `UsageFault_Handler` | IOC:true | startup.s:201 | **[覆写]** [stm32f4xx_it.c:129](Core/Src/stm32f4xx_it.c#L129) → `while(1)` | 用法错误 |
| 0x002C | `SVC_Handler` | IOC:true | startup.s:206 | **[覆写]** [stm32f4xx_it.c:144](Core/Src/stm32f4xx_it.c#L144) → 空 | SVC 系统调用（FreeRTOS 用） |
| 0x0030 | `DebugMon_Handler` | IOC:true | startup.s:210 | **[覆写]** [stm32f4xx_it.c:157](Core/Src/stm32f4xx_it.c#L157) → 空 | 调试监控 |
| 0x0038 | `PendSV_Handler` | IOC:true | startup.s:215 | **[覆写]** [stm32f4xx_it.c:170](Core/Src/stm32f4xx_it.c#L170) → 空 | PendSV（FreeRTOS 用） |
| **0x003C** | **`SysTick_Handler`** | IOC:true | startup.s:219 | **[覆写]** [stm32f4xx_it.c:183](Core/Src/stm32f4xx_it.c#L183) → `HAL_IncTick()` | **系统滴答（1ms）** |
| **0x0118** | **`USART3_IRQHandler`** | **IOC:true** (NVIC 优先级 0:0) | startup.s:347 | **[覆写]** [stm32f4xx_it.c:204](Core/Src/stm32f4xx_it.c#L204) → `HAL_UART_IRQHandler(&huart3)` | **HC-05 蓝牙接收** |
| 其他全部 | `xxx_IRQHandler` | 未使能 | startup.s:308-388 | **默认弱定义** → `Default_Handler → B .` (死循环) | 未使用的外设中断 |

> **说明**: 当前项目通过 CubeMX IOC 配置了 10 个系统异常（NMI/HardFault/MemManage/BusFault/UsageFault/SVC/DebugMon/PendSV/SysTick）+ 1 个外设中断（USART3），共 **11 个使能的 IRQ**。其中实际激活功能的有 **2 个**（SysTick → HAL_IncTick，USART3 → 蓝牙接收），其余系统异常为死循环保护占位。中断向量表方面与 v0.1 完全一致，无新增中断。

---

## 5. 关键数据结构定义及使用位置

### 5.1 `DHT11_Data_t` — 温湿度传感器数据

```c
// 定义: Hardware/dht11.h:31-39
typedef struct {
    uint8_t temperature;      // 温度整数部分 (℃)
    uint8_t temperature_dec;  // 温度小数部分
    uint8_t humidity;         // 湿度整数部分 (%)
    uint8_t humidity_dec;     // 湿度小数部分
} DHT11_Data_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [app_sensor.c:7](APP/app_sensor.c#L7) | `static DHT11_Data_t sensorData;` | 全局唯一传感器数据副本 |
| [app_sensor.c:30](APP/app_sensor.c#L30) | `DHT11_Read(&sensorData)` | 由 DHT11 驱动填充 |
| [app_sensor.c:51](APP/app_sensor.c#L51) | `const DHT11_Data_t *APP_Sensor_GetData()` | 返回只读指针给显示/协议层 |
| [app_display.c:11](APP/app_display.c#L11) | `static DHT11_Data_t lastSensor;` | 显示层缓存上次值，用于变化检测 |
| [app_display.c:63-69](APP/app_display.c#L63) | `sensor->temperature / humidity` | `APP_Display_IsChanged()` 变化检测 |
| [app_display.c:100-143](APP/app_display.c#L100) | `sensor->temperature / humidity` | ShowHomePage / ShowSensorPage 渲染 |
| [app_protocol.c:225-260](APP/app_protocol.c#L225) | `sensor->temperature / humidity` | STATUS 命令读取传感器数据 |

### 5.2 🆕 `APP_ConfigStatus_t` — 配置操作状态码

```c
// 定义: APP/app_status.h:5-25  (🆕 v0.2 新增文件)
typedef enum {
    APP_CONFIG_OK = 0,              // 操作成功
    APP_CONFIG_ERROR_MAGIC,         // Flash Magic 号不匹配
    APP_CONFIG_ERROR_VERSION,       // 配置版本不匹配
    APP_CONFIG_ERROR_LENGTH,        // 配置数据长度不匹配
    APP_CONFIG_ERROR_CRC,           // CRC32 校验失败
    APP_CONFIG_ERROR_VALUE,         // 参数值超出合法范围
    APP_CONFIG_ERROR_FLASH_Erase,   // Flash 扇区擦除失败
    APP_CONFIG_ERROR_FLASH_Write    // Flash 写入失败
} APP_ConfigStatus_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [app_config.c:21](APP/app_config.c#L21) | `APP_ConfigStatus_t status;` | 接收 Load 返回值 |
| [app_config.c:31-130](APP/app_config.c#L31) | `return APP_CONFIG_ERROR_* / APP_CONFIG_OK` | Load() 各校验分支返回对应错误码 |
| [app_config.c:138-192](APP/app_config.c#L138) | `return APP_CONFIG_ERROR_* / APP_CONFIG_OK` | Save() 各步骤返回对应错误码 |
| [app_config.c:36](APP/app_config.c#L36) | `APP_System_SetConfigStatus(status)` | 将 Load 结果写入全局系统状态 |
| [app_system.c:9](APP/app_system.c#L9) | `static APP_ConfigStatus_t configStatus;` | 全局配置状态存储 |
| [app_system.c:22](APP/app_system.c#L22) | `configStatus = APP_CONFIG_ERROR_MAGIC;` | 初始值：假设 Flash 中无有效配置 |
| [app_system.c:71-82](APP/app_system.c#L71) | `SetConfigStatus / GetConfigStatus` | 状态读写接口 |
| [app_display.c:164-167](APP/app_display.c#L164) | `APP_System_GetConfigStatus() == APP_CONFIG_OK ? "OK":"ERR"` | SYSTEM 页配置状态行 |
| [app_protocol.c:226-253](APP/app_protocol.c#L226) | `cfg == APP_CONFIG_OK ? "CONFIG:FLASH":"CONFIG:DEFAULT"` | STATUS 命令显示配置来源 |
| [app_protocol.c:177](APP/app_protocol.c#L177) | `ret == APP_CONFIG_OK` | INTERVAL 命令判断保存是否成功 |

### 5.3 🔧 `APP_ConfigData_t` — 运行参数配置

```c
// 定义: APP/app_config.h:16-20  (🔧 v0.2 字段重命名)
typedef struct {
    uint32_t sensorIntervalMs;   // 传感器采样间隔 (ms)
                                 // 🔧 v0.1: sensorInterval → v0.2: sensorIntervalMs
                                 // 默认 1000，合法范围 [500, 60000]
} APP_ConfigData_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [app_config.c:8](APP/app_config.c#L8) | `static APP_ConfigData_t appConfig;` | 全局唯一配置实例（RAM） |
| [app_config.c:45-46](APP/app_config.c#L45) | `appConfig.sensorIntervalMs = 1000` | 重置为默认值 |
| [app_config.c:115-128](APP/app_config.c#L115) | `memcpy(&appConfig, &storage.data, sizeof)` | 从 Flash 恢复配置 |
| [app_config.c:141-159](APP/app_config.c#L141) | `memcpy(&storage.data, &appConfig, sizeof)` | 写入前打包到存储结构 |
| [app_config.c:199-207](APP/app_config.c#L199) | `appConfig.sensorIntervalMs = ms` | 设置（下限钳位 500ms） |
| [app_config.c:214-217](APP/app_config.c#L214) | `return appConfig.sensorIntervalMs;` | 读取 |
| [app_config.c:271-279](APP/app_config.c#L271) | 🆕 `config->sensorIntervalMs < 500 \|\| > 60000` | APP_Config_Check() 范围校验 |
| [app.c:44](APP/app.c#L44) | `APP_Config_GetSensorInterval()` | 主循环中控制采样节奏 |
| [app_protocol.c:173](APP/app_protocol.c#L173) | `APP_Config_SetSensorInterval(sec * 1000)` | INTERVAL 命令修改 |
| [app_protocol.c:270](APP/app_protocol.c#L270) | `APP_Config_GetSensorInterval() / 1000` | STATUS 命令显示当前间隔 |

### 5.4 🆕 `APP_ConfigStorage_t` — Flash 存储布局

```c
// 定义: APP/app_config.h:22-35  (🆕 v0.2 新增)
typedef struct {
    uint32_t magic;              // 魔数 0x53484F4D ("SHOM")
    uint16_t version;            // 配置版本号 (当前=1)
    uint16_t length;             // APP_ConfigData_t 的大小
    APP_ConfigData_t data;       // 配置数据本体
    uint32_t crc;                // data 的 CRC32 校验值
} APP_ConfigStorage_t;
```

> **Flash 布局** (Sector7, 0x08060000, 128KB):
> ```
> [magic:4B] [version:2B] [length:2B] [data:N B] [crc:4B]
> ```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [app_config.c:55-60](APP/app_config.c#L55) | `FLASH_Read(FLASH_CONFIG_ADDRESS, &storage, sizeof)` | 从 Flash 加载 |
| [app_config.c:67-118](APP/app_config.c#L67) | `storage.magic / .version / .length / .crc` | 五重校验 |
| [app_config.c:140-166](APP/app_config.c#L140) | 逐字段赋值打包 | 保存前构建存储结构 |
| [app_config.c:182-186](APP/app_config.c#L182) | `FLASH_Write(FLASH_CONFIG_ADDRESS, &storage, sizeof)` | 写入 Flash |

### 5.5 `APP_DisplayPage_t` — 显示页面枚举

```c
// 定义: APP/app_display.h:10-22  (未变更)
typedef enum {
    DISPLAY_PAGE_HOME = 0,    // 首页：温湿度 + LED 状态
    DISPLAY_PAGE_SENSOR,      // 传感器页：仅温湿度
    DISPLAY_PAGE_SYSTEM,      // 系统页：LED / HC05 / CFG 状态  🔧 v0.2: 内容变更
    DISPLAY_PAGE_DEBUG,       // 调试页：Tick / 模式
    DISPLAY_PAGE_MAX          // 页面总数 (哨兵值)
} APP_DisplayPage_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [app_display.c:18-20](APP/app_display.c#L18) | `static APP_DisplayPage_t currentPage / lastPage` | 当前页 + 上次页（变化检测） |
| [app_display.c:203-232](APP/app_display.c#L203) | `switch(currentPage)` | 页面路由 → 调用对应 Show 函数 |
| [app_display.c:239-246](APP/app_display.c#L239) | `APP_Display_SetPage(page)` | 设置页面（含边界检查） |
| [app_protocol.c:124-141](APP/app_protocol.c#L124) | `APP_Display_SetPage(HOME/SENSOR/SYSTEM/DEBUG)` | PAGE 命令设置页面 |
| [app_protocol.c:202-220](APP/app_protocol.c#L202) | `APP_PageToString(page)` | STATUS 命令把页面枚举转字符串 |

### 5.6 `APP_LED_State_t` — LED 状态枚举

```c
// 定义: APP/app_control.h:6-10  (未变更)
typedef enum {
    APP_LED_OFF = 0,
    APP_LED_ON
} APP_LED_State_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [app_control.c:4](APP/app_control.c#L4) | `static APP_LED_State_t ledState;` | 当前 LED 状态 |
| [app_control.c:18-29](APP/app_control.c#L18) | `APP_Control_LED(state)` | 根据枚举控制 GPIO |
| [app_control.c:35-38](APP/app_control.c#L35) | `APP_Control_GetLEDState()` | 返回状态（供显示/协议查询） |
| [app_display.c:13,73,111,153](APP/app_display.c#L13) | `lastLedState` + 比对 + 显示 | 变化检测 + HOME/SYSTEM 页渲染 |
| [app_protocol.c:263-265](APP/app_protocol.c#L263) | `APP_Control_GetLEDState()` | STATUS 命令读取 |

### 5.7 `APP_Command_t` — 命令表条目

```c
// 定义: APP/app_protocol.h:10-16  (未变更)
typedef struct {
    const char *cmd;              // 命令字符串（如 "LED"、"PAGE"）
    void (*handler)(char *param); // 处理函数指针（param 为空格后的参数）
} APP_Command_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [app_protocol.c:281-296](APP/app_protocol.c#L281) | `static const APP_Command_t APP_CommandTable[7]` | 7 条命令的注册表 |
| [app_protocol.c:321-330](APP/app_protocol.c#L321) | `strcmp(cmd, APP_CommandTable[i].cmd)` → `.handler(param)` | 解析时遍历匹配并调用 |

### 5.8 `HC05_RingBuffer_t` — 蓝牙接收环形缓冲区

```c
// 定义: Hardware/hc05.h:22-33  (未变更)
typedef struct {
    uint8_t buffer[128];         // 128 字节数据缓冲区
    volatile uint16_t head;      // 写指针（ISR 中修改）
    volatile uint16_t tail;      // 读指针（主循环中修改）
} HC05_RingBuffer_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [hc05.c:9](Hardware/hc05.c#L9) | `static HC05_RingBuffer_t HC05_RxBuffer;` | 全局唯一实例 |
| [hc05.c:73-93](Hardware/hc05.c#L73) | `HC05_BufferWrite(data)` | ISR 回调中写入 |
| [hc05.c:98-114](Hardware/hc05.c#L98) | `HC05_BufferRead(data)` | APP_Protocol_Process 中读取 |
| [hc05.c:60-68](Hardware/hc05.c#L60) | `HC05_BufferInit()` | head=0, tail=0, memset 清零 |

### 5.9 🆕 `FLASH_Status_t` — Flash 操作状态

```c
// 定义: Hardware/flash.h:19-24  (🆕 v0.2 新增)
typedef enum {
    FLASH_OK = 0,
    FLASH_ERROR
} FLASH_Status_t;
```

| 使用位置 | 方式 | 说明 |
|---------|------|------|
| [flash.c:5-41](Hardware/flash.c#L5) | `FLASH_EraseConfigSector()` | 擦除 Sector7，返回 FLASH_OK/ERROR |
| [flash.c:46-97](Hardware/flash.c#L46) | `FLASH_Write()` | 按字编程，返回 FLASH_OK/ERROR |
| [app_config.c:173-193](APP/app_config.c#L173) | `if(FLASH_EraseConfigSector() != FLASH_OK)` | Config Save 时调用 |

---

## 6. 硬件引脚映射

| 外设 | 引脚 | GPIO | 功能 | CubeMX 信号 | 备注 |
|------|------|------|------|------------|------|
| LED | PC13 | GPIOC PIN13 | 板载 LED (推挽输出) | GPIO_Output | 初始 LOW（点灯）；CubeMX 锁定, 不可修改 |
| DHT11 | PA3 | GPIOA PIN3 | 单总线数据 | — | 未在 IOC 配置（dht11.c 动态切换输入/输出） |
| OLED (I2C1) | PB6 | I2C1_SCL | I2C 时钟 (AF_OD, AF4) | I2C1_SCL | 100kHz |
| OLED (I2C1) | PB7 | I2C1_SDA | I2C 数据 (AF_OD, AF4) | I2C1_SDA | 地址 `0x3C<<1 = 0x78` |
| USART1 (调试) | PA9 | USART1_TX | 调试串口发送 (AF_PP, AF7) | USART1_TX | 115200-8-N-1 |
| USART1 (调试) | PA10 | USART1_RX | 调试串口接收 (AF_PP, AF7) | USART1_RX | printf 重定向 |
| USART3 (蓝牙) | PB10 | USART3_TX | HC-05 发送 (AF_PP, AF7) | USART3_TX | 9600-8-N-1 |
| USART3 (蓝牙) | PB11 | USART3_RX | HC-05 接收 (AF_PP, AF7) | USART3_RX | 中断接收 (NVIC 优先级 0:0) |
| SWD 调试 | PA13 | SYS_JTMS-SWDIO | SWD 数据线 | Serial_Wire | 调试接口 |
| SWD 调试 | PA14 | SYS_JTCK-SWCLK | SWD 时钟线 | Serial_Wire | 调试接口 |
| HSE 晶振 | PH0 | RCC_OSC_IN | 8MHz 外部晶振输入 | HSE-External-Oscillator | PLL → 168MHz |
| HSE 晶振 | PH1 | RCC_OSC_OUT | 8MHz 外部晶振输出 | HSE-External-Oscillator | — |

> **v0.2 引脚变更**: 无。与 v0.1 完全一致，IOC 仍为 12 个引脚、6 个 IP（I2C1 / NVIC / RCC / SYS / USART1 / USART3）。PA3 (DHT11) 未在 IOC 中静态配置，由 dht11.c 运行时动态切换 GPIO 方向。

### 内存映射

| 区域 | 起始地址 | 大小 | 用途 |
|------|---------|------|------|
| Flash (主程序) | 0x08000000 | 384KB (Sector 0~6) | 代码 + 只读数据 |
| 🆕 Flash (配置存储) | **0x08060000** | 128KB (Sector 7) | `APP_ConfigStorage_t` 持久化 |
| Flash (总容量) | 0x08000000 | 512KB (Sector 0~7) | STM32F407VET6 |
| SRAM1 | 0x20000000 | 112KB | RW + ZI 数据 |
| SRAM2 (CCM) | 0x2001C000 | 16KB | 未使用（分散加载已预留） |

---

> **文档结束** — 本文档基于 2026-07-29 代码快照全量重新扫描生成（版本 v0.2），覆盖 12 个源文件（3 新增 + 6 修改 + 3 未变）。后续代码变更请同步更新对应章节。
